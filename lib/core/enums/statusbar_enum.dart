/// An enum that represents the status bar theme.
enum StatusbarType {
  /// An enum to represent if the status bar should be light.
  light,

  /// An enum to represent if the status bar should be light.
  dark,
}
